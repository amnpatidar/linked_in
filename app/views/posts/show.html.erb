<h1>show post</h1>

<p style="font-size: large;"> <%= @post.article %> </p>
<p>
<% if @post.image.attached? %>
	<%= image_tag @post.image,width: 400 %> 
<% end %>
</p>
<% if @post.postable_id == current_user.id %>
<%# byebug %>
	 <%= button_to "Edit", [:edit, @post], method: :get %> <br>
	<p> <%= button_to "Destroy", post_path(@post),method: :delete %></p>
<% end %>

<p>
	<%= render  @commentable.comments%>
</p>

<p>
	<%= render 'comments/form' %>
</p> 